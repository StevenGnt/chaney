
/**
 * Return the sum of two amounts (may be any number, int, float ...)
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function addAmounts(a, b) {
    return (parseFloat(a) + parseFloat(b)).toFixed(2);
}
